@model ProyectoFinalDAMAgil.Models.Admin.AlumnoModel

@{
    ViewData["Title"] = "Editar Alumno";
}

<div class="container mt-5">
    <div class="card">
        <div class="card-header">
            <h3>Editar Alumno</h3>
        </div>
        <div class="card-body">
            <form asp-controller="Admin" asp-action="EditarAlumno" method="post">
                <input type="hidden" asp-for="IdAlumno" />
                <input type="hidden" asp-for="IdCentro" />
                <input type="hidden" asp-for="Email" />

                <div class="mb-3">
                    <label asp-for="NombreUsuario" class="form-label">Nombre Alumno</label>
                    <input asp-for="NombreUsuario" type="text" class="form-control" />
                    <span asp-validation-for="NombreUsuario" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="ApellidosUsuario" class="form-label">Apellidos Alumno</label>
                    <input asp-for="ApellidosUsuario" type="text" class="form-control" />
                    <span asp-validation-for="ApellidosUsuario" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="FechaDeNacimiento" class="form-label">Fecha de Nacimiento</label>
                    <input asp-for="FechaDeNacimiento" type="date" class="form-control" value="2000-01-01" min="1900-01-01" />
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Guardar
                    </button>
                    <a asp-controller="Admin" asp-action="ListarAlumnos" asp-route-id="@Model.IdCentro" class="btn btn-warning">
                        <i class="bi bi-arrow-left"></i> Volver Lista Alumnos
                    </a>
                </div>
            </form>
            @if (!string.IsNullOrEmpty(ViewData["Mensaje"] as string))
            {
                <div class="alert alert-danger mt-3" role="alert">
                    @{
                        string vartempmensaje = ViewData["Mensaje"] as string;
                    }

                    @vartempmensaje
                </div>
            }
        </div>
    </div>
</div>
