@model ProyectoFinalDAMAgil.Models.Admin.ProfesorModel

@{
    ViewData["Title"] = "Editar Profesor";
}

<div class="container mt-5">
    <div class="card">
        <div class="card-header">
            <h3>Editar Alumno</h3>
        </div>
        <div class="card-body">
            <form asp-controller="Admin" asp-action="EditarProfesor" method="post">
                <input type="hidden" asp-for="IdProfesor" />
                <input type="hidden" asp-for="IdCentro" />
                <input type="hidden" asp-for="Email" />

                <div class="mb-3">
                    <label class="form-label" for="NombreUsuario">Nombre Profesor</label>
                    <input asp-for="NombreUsuario" type="text" class="form-control" id="NombreUsuario" placeholder="Nombre del Profesor">
                    <span asp-validation-for="NombreUsuario" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="ApellidosUsuario">Apellidos Profesor</label>
                    <input asp-for="ApellidosUsuario" type="text" class="form-control" id="ApellidosUsuario" placeholder="Apellidos Profesor">
                    <span asp-validation-for="ApellidosUsuario" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label class="form-label" for="Especialidad">Especialidad</label>
                    <input asp-for="Especialidad" type="text" class="form-control" id="Especialidad" placeholder="Especialidad">
                    <span asp-validation-for="Especialidad" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Guardar
                    </button>
                    <a asp-controller="Admin" asp-action="ListarProfesores" asp-route-id="@Model.IdCentro" class="btn btn-warning">
                        <i class="bi bi-arrow-left"></i> Volver Lista Alumnos
                    </a>
                </div>
            </form>
            @if (!string.IsNullOrEmpty(ViewData["Mensaje"] as string))
            {
                <div class="alert alert-danger mt-3" role="alert">
                    @{
                        string vartempmensaje = ViewData["Mensaje"] as string;
                    }

                    @Localizer[vartempmensaje]
                </div>
            }
        </div>
    </div>
</div>
